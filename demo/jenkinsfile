pipeline {
    agent {
        node {
            label "maven-build-server"
        }
    }

    tools {
        jdk "java-17"
        maven "maven-3.9.11"
    }

    stages {
        stage('Clean and Build the App') {
            steps {
                sh 'mvn clean install'
            }
        }
    }
}